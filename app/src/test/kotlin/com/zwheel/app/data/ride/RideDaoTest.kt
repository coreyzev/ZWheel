package com.zwheel.app.data.ride

// TODO(m3): Add in-memory DAO tests once Robolectric JUnit5 extension is wired.
// Blocked by: Robolectric requires @RunWith(RobolectricTestRunner::class) which is JUnit4-only;
// JUnit5 support needs robolectric-junit-platform (available in Robolectric 4.13+) but
// the integration requires additional test config not yet present.
// Schema and DAO correctness is verified by the KSP Room compiler at build time.
