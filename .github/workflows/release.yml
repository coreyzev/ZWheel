name: Release

on:
  workflow_dispatch:

permissions:
  contents: write
  actions: read

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Check out
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check latest CI run on main is green
        id: ci-run
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          read run_id conclusion <<< $(gh run list \
            --repo ${{ github.repository }} \
            --workflow=ci.yml \
            --branch=main \
            --limit=1 \
            --json databaseId,conclusion \
            --jq '.[0] | "\(.databaseId) \(.conclusion)"')
          if [ -z "$run_id" ] || [ "$run_id" = "null" ]; then
            echo "No CI run found on main" >&2
            exit 1
          fi
          if [ "$conclusion" != "success" ]; then
            echo "Latest CI run $run_id on main is '$conclusion', not 'success' — refusing to release" >&2
            exit 1
          fi
          echo "run_id=$run_id" >> $GITHUB_OUTPUT
          echo "Latest CI run $run_id is green — proceeding"

      - name: Download APK from CI
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh run download ${{ steps.ci-run.outputs.run_id }} \
            --repo ${{ github.repository }} \
            --name=zwheel-debug \
            --dir=./artifacts
          mv artifacts/app-debug.apk zwheel-debug.apk

      - name: Tag build
        run: |
          git tag "build-${{ github.run_number }}"
          git push origin "build-${{ github.run_number }}"

      - name: Publish release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: "build-${{ github.run_number }}"
          name: "Debug Build #${{ github.run_number }}"
          body: "Built from CI run ${{ steps.ci-run.outputs.run_id }} on `main`."
          files: zwheel-debug.apk
          prerelease: true
          make_latest: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Prune old releases (keep last 2)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release list --limit 100 --json tagName --jq '.[2:][].tagName' | \
            while IFS= read -r tag; do
              gh release delete "$tag" --yes --cleanup-tag || true
            done
